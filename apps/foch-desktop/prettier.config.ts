import type { Config } from "prettier";

const config: Config = {
	printWidth: 88,
	proseWrap: "always",
	semi: true,
	singleQuote: false,
	trailingComma: "all",
	useTabs: true,
};

export default config;

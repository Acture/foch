namespace = sample
country_event = {
  id = sample.1
  title = sample.1.t
  category = ADM
  target = root
  trigger = {
    has_country_flag = demo_flag
  }
  immediate = {
    add_prestige = 5
  }
}
